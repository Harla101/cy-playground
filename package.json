{
  "name": "cy-playground",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --port=5005",
    "build": "vite build",
    "preview": "vite preview --port=5005",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:ci": "npm build && npm preview & wait-on http://localhost:5005 & npm cypress:run"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "@vitejs/plugin-react": "^1.3.0",
    "cypress": "^9.6.1",
    "vite": "^2.9.9",
    "wait-on": "^6.0.1"
  }
}
